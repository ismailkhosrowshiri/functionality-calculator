const patentElementIndex = document.querySelector(".btn-area");
patentElementIndex.addEventListener("click", function (event) {
  if (event.target.classList.contains("secend-row-btn2")) {
    const x = "div";
    x(x);
    console.log(x);
  }
});
function x(x) {
  if (x === "div" || x === "tagsim") {
    for (let i = 0; i < arr.length; i++) {
      let firstVal = (arr[i].value = firstVal);
    }
  }
}
let arr = ["1", "2"];
console.log(firstVal);
